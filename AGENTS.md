# Project Instructions

Do not create a Python virtual environment.

Do not create a .venv directory.

Assume Python dependencies are managed externally.

Use the system Python or the existing interpreter.
